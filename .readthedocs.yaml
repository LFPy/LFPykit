version: 2

sphinx:
  configuration: doc/source/conf.py

formats: all

build:
  os: ubuntu-lts-latest
  tools:
    python: "3.10"

conda:
  environment: doc/source/environment.yml

python:
  install:
    - method: pip
      path: .
